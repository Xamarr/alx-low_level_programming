#include <stdio.h>
/**
 * main - prints the alphabet 
 *
 * Description: using the main fumction
 * this program prints the alphabets
 * Return: 0
 */
int main(void)
{
	char ch;

	for (c = 'a'; c <= 'z'; c++)
		putchar(ch);

	putchar('\n');
	return(0);
}
